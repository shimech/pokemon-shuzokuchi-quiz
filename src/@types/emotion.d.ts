import '@emotion/react';

declare module '@emotion/react' {
  interface Theme {
    colors: {
      black: string;
      white: string;
      gray: string;
      darkGray: string;
      red: string;
      blue: string;
      purple: string;
    };
  }
}
